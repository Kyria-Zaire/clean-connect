import { Module } from '@nestjs/common'

/**
 * Placeholder Auth module — implémentation complète au PRD-001 (JWT + refresh + RBAC).
 * Cf. cahier v1.4 §3.1 et rule securite.
 */
@Module({})
export class AuthModule {}
